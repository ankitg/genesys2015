import types from "constants/ActionTypes";

const initialState = {
	transactionHistory: []
};

export default function account(state = initialState, action = {}) {
	switch (action.type) {
	
	default:
		return state;
	}
}
